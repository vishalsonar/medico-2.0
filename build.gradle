plugins {
  id "org.sonarqube" version "3.4.0.2513"
}

sonarqube {
  properties {
    property "sonar.projectKey", "vishalsonar_medico-2.0"
    property "sonar.organization", "vishalsonar"
    property "sonar.host.url", "https://sonarcloud.io"
  }
}